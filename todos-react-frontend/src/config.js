const config = {
    API_URL: process.env.API_URL || "https://zv0dtp38xl.execute-api.eu-west-1.amazonaws.com/prod",
    APP_NAME: process.env.APP_NAME || "Todo App",
    APP_VERSION: process.env.APP_VERSION || "1.0.0",
};

export default config;